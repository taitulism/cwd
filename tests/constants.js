const path = require('path');

const TEST_DIR = path.resolve(__dirname, './dummy-dir');
const OTHER_TEST_DIR = path.resolve(__dirname, './other-dummy-dir');

module.exports = {
	TEST_DIR,
	OTHER_TEST_DIR,
};
